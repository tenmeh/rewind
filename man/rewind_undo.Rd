% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{rewind_undo}
\alias{rewind_undo}
\alias{rewind_redo}
\alias{rewind_jump}
\alias{rewind_clear}
\title{Move through the history programmatically}
\usage{
rewind_undo(session = shiny::getDefaultReactiveDomain())

rewind_redo(session = shiny::getDefaultReactiveDomain())

rewind_jump(index, session = shiny::getDefaultReactiveDomain())

rewind_clear(session = shiny::getDefaultReactiveDomain())
}
\arguments{
\item{session}{The Shiny session. Defaults to the current one.}

\item{index}{Position to jump to, 1-based, where 1 is the oldest retained
entry.}
}
\value{
\code{TRUE} if the pointer moved, \code{FALSE} otherwise, invisibly.
}
\description{
The keyboard shortcuts and \code{\link[=rewind_buttons]{rewind_buttons()}} cover the common cases; these
let you drive the history from your own controls.
}
\examples{
if (interactive()) {
  library(shiny)

  server <- function(input, output, session) {
    rewind_enable()
    observeEvent(input$my_back_button, rewind_undo())
  }
}
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diff.R
\name{rewind_diff}
\alias{rewind_diff}
\title{Show what changed between two steps}
\usage{
rewind_diff(
  from = NULL,
  to = NULL,
  session = shiny::getDefaultReactiveDomain()
)
}
\arguments{
\item{from, to}{The two positions to compare, as in the \code{index} column of
\code{\link[=rewind_history]{rewind_history()}}. The default compares the current position with the
one before it. Give both to compare any two positions.}

\item{session}{The Shiny session. The default is the current session.}
}
\value{
A data frame with one row for each value that differs, in the
order of the names. It has six columns:
\itemize{
\item \code{from}, \code{to}: the two positions compared.
\item \code{name}: the input ID, or \code{id$field} for a tracked value.
\item \code{change}: a short description that a person can read.
\item \code{old}, \code{new}: list columns that hold the values themselves.
}

The data frame has no rows when nothing differs, and when there is no
earlier step to compare with. It depends on the history, so you can
use it in \verb{render*()} and \code{observe()}.
}
\description{
\code{\link[=rewind_history]{rewind_history()}} gives one row for each step, with a label such as
\code{"region, year"}. That label says which values changed. This function
gives the values themselves: what each one held before, and what it
holds now.
}
\details{
Use it to tell the user what an undo will do, or to write your own
record of what a person changed.
}
\section{This is not an audit trail}{
The history lives in the memory of one session, and it goes when the
session goes. It also drops the oldest entries when it passes \code{depth},
and it drops the steps in front of the position when a change arrives
after an undo. A record for an inspection must be written as each change
occurs, and kept somewhere else. The history is reactive, so:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{observeEvent(rewind_history(), \{
  write_my_audit_row(rewind_diff())
\})
}\if{html}{\out{</div>}}
}

\section{Names, and not labels}{
The \code{name} column holds the input ID, such as \code{region}. It does not hold
the label of the widget, because Shiny does not give the server the
label of an input. A value that \code{\link[=rewind_track]{rewind_track()}} records appears as
\code{id$field}.
}

\section{The values can be any type}{
\code{old} and \code{new} are list columns, so they hold the values themselves,
whatever their type. The \code{change} column is a short description for a
person to read. It shows both values when they are short, such as
\code{"North -> South"}, and a count when they are not, such as
\code{"12 values"}.
}

\examples{
if (interactive()) {
  library(shiny)

  ui <- fluidPage(
    rewind_buttons(),
    selectInput("region", "Region", c("North", "South", "East")),
    sliderInput("year", "Years", 2018, 2026, c(2018, 2026), sep = ""),
    tableOutput("changed")
  )

  server <- function(input, output, session) {
    rewind_enable()

    # What did the last step change?
    output$changed <- renderTable({
      rewind_diff()[, c("name", "change")]
    })
  }

  shinyApp(ui, server)
}
}
